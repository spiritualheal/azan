# GrandMasterHeal - Independent Deployment Configuration
# 
# This project is fully independent and not dependent on any platform-specific
# configuration. All deployment instructions are in README.md
#
# For Replit users: This file is intentionally empty. Use README.md for setup.
# For other platforms: Follow the deployment guides in README.md for:
# - Railway, Heroku, DigitalOcean, or self-hosted VPS
#
# Environment Variables Required:
# - DATABASE_URL: PostgreSQL connection string (see .env.example)
# - PORT: Server port (default 5000)
# - NODE_ENV: 'development' or 'production'
#
# Quick Start (any platform):
# 1. npm install
# 2. cp .env.example .env.local (fill in your values)
# 3. npm run db:push
# 4. npm run dev (development) or npm run build && npm run start (production)
