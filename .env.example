# Environment Configuration for GrandMasterHeal
# Copy this file to .env.local and fill in your actual values

# Database Configuration (PostgreSQL)
# Get a free PostgreSQL database from services like:
# - Neon (https://neon.tech) - serverless PostgreSQL
# - Railway (https://railway.app) - PostgreSQL hosting
# - DigitalOcean (https://digitalocean.com) - managed databases
DATABASE_URL=postgresql://user:password@localhost:5432/grandmasterheal

# Server Configuration
# Port to run the application on (default: 5000)
PORT=5000

# Node Environment
# Set to 'production' for deployment, 'development' for local development
NODE_ENV=development

# CORS and Security (if needed for production)
# ALLOWED_ORIGINS=https://yourdomain.com

# Email Configuration (optional, for contact form notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Session Secret (generate a random string for production)
# SESSION_SECRET=your-random-secret-key-here

# Analytics and Tracking (optional)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXXXXX
